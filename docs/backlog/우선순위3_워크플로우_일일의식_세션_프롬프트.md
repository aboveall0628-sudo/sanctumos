# 우선순위 3 — 워크플로우 + 일일 의식 세션 프롬프트
**작성일**: 2026-05-13
**용도**: 별도 세션(들)에서 워크플로우 모델 + 일일 의식 화면 작업할 때 첫 입력으로 사용

---

## 사용법
이 작업은 **2~3 세션** 분량이라 단계별로 진행합니다. 첫 세션 시작 시 아래 프롬프트를 복사해 붙여넣으세요. 두 번째 이후 세션은 어느 단계까지 끝났는지 확인하고 다음 단계 진입.

**선행 권장**: 우선순위 2 (source 메타 인프라) 먼저 끝낸 후 진입. 안 하면 도트 보강 시 source 필드 마이그레이션 같이 해야 해서 작업 복잡도 증가.

---

## 프롬프트 본문 (복사용)

```
Sanctum OS의 워크플로우 모델 + 일일 의식 화면 구현 작업을 시작합니다.

## 작업 맥락

2026-05-13 기획 세션에서 합의된 거대한 유기체 비전의 핵심 매듭:
- 워크플로우(Workflow): 목표를 도트로 분해하는 다리 — 현재 시스템의 가장 큰 빈자리
- 일일 의식(Daily Ritual): 매일 저녁 묵상 후 내일 도트 분배하는 흐름 — 신규 화면

기획서 전체 참조: docs/backlog/목표_워크플로우_일일의식_기획서.md

## 권장 단계 (2~3 세션)

**단계 1: 데이터 모델 신규 + 도트 보강 (1 세션)**
- Workflow 모델 신규
- GoalVersion 모델 신규 (시점 스냅샷 방법 A)
- 도트 모델 보강: linkedWorkflowStepId, executor, helperPersonId, goalVersionId 필드
- 목표 → 도트 역참조 인덱싱 (Firestore 인덱스 회피 패턴 준수)

**단계 2: 일일 의식 화면 — 웹 (1 세션)**
- 좌-우 2패널 구조
- 좌: 워크플로우 스텝 목록 (체크리스트 + 시간 박스 결합, 예상 도트 수 표시)
- 우: 내일 시간표 (드래그 앤 드롭)
- 묵상 화면에서 진입 경로

**단계 3: 일일 의식 화면 — 앱 (1 세션)**
- 2탭 스와이프 구조 (스텝 탭 ↔ 시간표 탭)
- 스텝에서 [+] 누르면 시간표 탭으로 자동 이동
- 모바일 터치 인터랙션 최적화

## 작업 목표 (전체)

| 항목 | 설명 |
|---|---|
| Workflow 모델 | parentGoalId / steps[] / generatedByDecision / revisionLog |
| Workflow.steps | id / order / title / estimatedDots / executor / status / linkedDotIds |
| GoalVersion 모델 | goalId / versionNumber / snapshotData / validFrom~To / revisionReason |
| 도트 보강 | linkedWorkflowStepId / executor (self|helper|external) / helperPersonId / goalVersionId |
| 역참조 | 목표 ID로 도트 빠르게 조회 가능하게 |
| 일일 의식 웹 | 좌-우 2패널, 드래그 앤 드롭 |
| 일일 의식 앱 | 2탭 스와이프 |

## 필수 참조

세션 시작 시 다음을 확인:
- docs/backlog/목표_워크플로우_일일의식_기획서.md (전체, 특히 §5 §9)
- docs/backlog/B1_의사결정_시스템_기획서.md — 의사결정 게이트 흐름 (목표 수정 시)
- 메모리 feedback_organism_architecture.md — 거대한 유기체 흐름
- 메모리 feedback_evaluation_vs_causation.md — 시점 스냅샷 + 인과 가지 원칙
- 메모리 feedback_return_to_word.md — 묵상으로 돌아가기 (일일 의식 진입점)
- 메모리 feedback_firestore_index_pattern.md — composite index 회피
- 코드: seeds.js (10년→연간→분기→주간 시드 데이터, 7계층 목표 이미 존재)
- 코드: 기존 도트 모델 + 목표 모델 정의

## 작업 흐름 (Rule 9 준수)

큰 변경 전 질의응답으로 합의:
1. **이번 세션의 단계** — 1·2·3 중 어디까지 할지 사용자와 합의
2. **데이터 모델 마이그레이션** — 기존 도트에 새 필드 추가 시 기본값 정책
3. **GoalVersion 생성 트리거** — 목표 수정 시 자동? 명시적?
4. **UI 와이어프레임** — 화면 진입 전 사용자에게 ASCII 또는 설명으로 확인
5. **소스 메타 통합** — 우선순위 2 작업이 완료됐는지 확인. 안 됐으면 같이 처리

## 완료 기준 (단계별)

### 단계 1 완료
- [ ] Workflow / GoalVersion 모델 Firestore 컬렉션 정의
- [ ] 도트 모델 보강 + 기존 도트 마이그레이션
- [ ] 목표 → 도트 역참조 작동 확인
- [ ] 작은 테스트 케이스 1개 생성·확인
- [ ] 커밋 + main 푸시

### 단계 2 완료
- [ ] 일일 의식 웹 화면 작동
- [ ] 묵상 화면에서 진입 경로 연결
- [ ] 드래그 앤 드롭으로 스텝 → 시간표 배치 가능
- [ ] 도트 생성 자동 (배치 결과)
- [ ] 브라우저 직접 테스트
- [ ] 커밋 + main 푸시

### 단계 3 완료
- [ ] 일일 의식 앱 화면 (모바일 뷰포트) 작동
- [ ] 2탭 스와이프 작동
- [ ] 스텝 [+] → 시간표 자동 이동
- [ ] 모바일 뷰포트 직접 테스트
- [ ] 커밋 + main 푸시

## 환경
- 작업 디렉토리: c:\Users\MSI\Desktop\개발\Sanctum OS
- 셸: PowerShell
- 기술 스택: Vanilla JS PWA, Firebase (biblealimi)
- 배포: main 푸시 = 즉시 GitHub Pages 반영

## 주의사항

- Firestore composite index 의존 금지 (feedback_firestore_index_pattern.md)
  → userId 단일 쿼리 + 클라이언트 처리 패턴 사용
- 모든 새 데이터 모델 필드에 source 메타 적용 (우선순위 2 결과)
- 도트 자동 분배는 금지 — 사용자 수동 분배만 (자동화 거부 정책)
- 모든 워크플로우 / 목표 변경에는 revisionLog 박을 것
- "거대한 유기체" 흐름 항상 의식 — 새 노드는 양방향 참조 우선

## 이 세션의 목표

위 단계 중 사용자와 합의한 단계까지 완료하고 main에 푸시. 다음 세션을 위해 어디까지 끝났는지 메모리에 기록.

먼저 현재 도트 모델·목표 모델·seeds.js 구조를 파악하고, 단계 1 시작을 위한 데이터 모델 합의를 질의응답으로 진행하세요.
```

---

## 세션 종료 시 업데이트해야 할 메모리
- project_status.md → 워크플로우/일일 의식 진행 단계 갱신
- 새 reference 메모로 "Workflow + GoalVersion 데이터 모델 정의" 박기 권장
- 단계 단위 완료 시마다 메모 갱신해 다음 세션에서 컨텍스트 잃지 않게
