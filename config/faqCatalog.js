/**
 * faqCatalog.js — 1차 베타 FAQ 카탈로그 (v73, 2026-05-18)
 *
 * 사용자 명시 (2026-05-18):
 *   "시안 그대로 가되, 그냥 묵상, 도트, 인물이라는 단어 다 빼고
 *    정보를 평문으로 저장하지 않는다고만 해줘.
 *    베타버전용 FAQ고, 그 외에 답변하지 못하는 부분에 대해서는
 *    개발자에게 직접 문의하게 해주고, 내가 그거에 답변할 수 있게 만들어줘."
 *
 * 두 자리에 같은 카탈로그 노출:
 *   1. SWAN 채팅 모달 안 — 헤더 아래 칩 row. 클릭 시 두 turn 자연 삽입 (질문 + 답변).
 *   2. 설정 [안내] 카테고리 안 — 아코디언 카드. 클릭 시 펼침.
 *
 * 답에 없는 자리는 자유 채팅 입력으로 흘러가 운영자(개발자)에게 직접 전달.
 *
 * 정식 출시 시 통째 갱신 예정 (1차 베타 한정 카피).
 */

export const FAQ_CATALOG = [
    {
        id: 'start',
        question: '시작은 어떻게 하나요?',
        answer:
            '사이드바 [오늘] 가장 위 \'오늘의 시작\' 카드에서 자연 보여요. ' +
            '한 줄만 적어도 충분해요. ' +
            '본인 페이스로 천천히 쌓아가시면 됩니다.',
    },
    {
        id: 'what-is',
        question: '이건 어떤 앱이에요?',
        answer:
            '매일 한 줄씩 쌓아가는 회고 시스템이에요. ' +
            '하루 한 점씩 적은 기록이 모이면 한 주·한 달·한 해 회고로 자연 묶여요.',
    },
    {
        id: 'weekly',
        question: '주간 리포트는 언제 만들어져요?',
        answer:
            '두 자리에서 만드실 수 있어요. ' +
            '① 토요일 회고 자리 하단 [이번 주 리포트 만들기] 버튼 ' +
            '② 사이드바 [리포트] → 주간 탭 가장 위 [이번 주 리포트 만들기] 버튼. ' +
            '두 자리 결과는 같아요.',
    },
    {
        id: 'password',
        question: '비밀번호 잃어버리면 어떻게 하나요?',
        answer:
            '두 가지 안전망이 있어요. ' +
            '① 가입할 때 받으신 24단어 복구코드 ' +
            '② 설정 [보안]에서 등록하신 이메일 복구. ' +
            '둘 중 하나만 있어도 다시 들어오실 수 있어요.',
    },
    {
        id: 'safety',
        question: '내가 적은 내용이 안전한가요?',
        answer:
            '사용자가 적으신 정보는 평문으로 저장하지 않아요. ' +
            '본인 비밀번호 없이는 운영자도 내용을 볼 수 없어요.',
    },
    {
        id: 'decision-gate',
        question: '분별의 자리는 어떨 때 쓰나요?',
        answer:
            '어떤 선택 앞에서 망설일 때 자리예요. ' +
            '5개 질문을 거치며 본인의 원칙·과거 기록과 비교해 결정을 다듬으실 수 있어요. ' +
            '(베타 메인 모드에서 보여요)',
        // (2026-05-18 v74) 분별의 자리는 슬림 모드에서 사이드바 자체가 hidden 인 모듈 →
        //   슬림 사용자에겐 FAQ 자리에서도 자연 숨김 (어디서 쓰는지 헷갈림 방지).
        slimHidden: true,
    },
    {
        id: 'after-beta',
        question: '베타 끝나면 어떻게 되나요?',
        answer:
            '그동안 적으신 모든 기록은 그대로 이어져요. ' +
            '베타 졸업 사용자에겐 정식 출시 후 ' +
            '무제한 이용권 + 다음 유료 기능 무료 등 보상이 함께해요.',
    },
];

/**
 * FAQ 에 없는 질문 안내 카피 — 두 자리에서 공통 사용.
 *   SWAN 채팅 자체가 운영자(개발자)에게 전달되는 통로라 별도 라우팅 X.
 */
export const FAQ_FALLBACK_HINT_CHAT = '여기 없는 질문은 그냥 채팅으로 보내주세요. 개발자가 직접 답변해 드려요.';
export const FAQ_FALLBACK_HINT_SETTINGS = '여기 없는 질문은 우하단 풍선(SWAN)으로 보내주세요. 개발자가 직접 답변해 드려요.';

/**
 * 카탈로그 조회 헬퍼.
 */
export function findFaqById(id) {
    return FAQ_CATALOG.find(f => f.id === id) || null;
}

/**
 * (2026-05-18 v74) 현재 tier 에 따라 노출할 FAQ 만 필터.
 *   슬림 모드(베타) — slimHidden:true 항목 자연 제거.
 *   풀모드 — 모두 노출.
 */
export function getVisibleFaqs() {
    const isSlim = typeof document !== 'undefined'
        && document.documentElement.dataset.tier === 'slim';
    return FAQ_CATALOG.filter(f => !(isSlim && f.slimHidden));
}
